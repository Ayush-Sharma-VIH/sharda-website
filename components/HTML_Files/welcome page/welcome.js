
window.onload = () => {
    const content = document.querySelector('.welcome-content');
    const image = document.querySelector('.image-section img');

    
    setTimeout(() => {
        image.classList.add('hover-animate');
    }, 800);
};
